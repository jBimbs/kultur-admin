# TODO

- [ ] Add search bar + filtering to `app/festivals/view-festivals/page.tsx` by copying layout from `app/currated-trails/view-currated-trails/page.tsx`.
- [ ] Update Supabase query to filter festivals by `name` using `ilike` when search term is present.
- [ ] Reset pagination (`setPage(0)`) when search term changes.
- [ ] Update total count query to respect the same filter.
- [ ] Manually verify pagination and filtering in the UI.
- [x] Added search bar + filtering to `app/festivals/view-festivals/page.tsx`.
- [x] Updated Supabase query to filter festivals by `name` using `ilike`.
- [x] Reset pagination when search term changes.
- [x] Updated total count to respect the search filter.


